// use crate::controllers::upload;
// use axum::{routing::post, Router};
//
// pub fn create_routes() -> Router {
//     Router::new().route("/upload", post(upload::upload_photo))
// }

pub fn testing() {
    println!("This is just testing");
}
